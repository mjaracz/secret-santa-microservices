spring.application.name=notification-service
spring.profiles.active=local

spring.kafka.bootstrap-servers=${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
spring.kafka.consumer.group-id=${KAFKA_CONSUMER_GROUP_ID:notification-service}
kafka.topics.notification-events=${KAFKA_TOPIC_NOTIFICATION_EVENTS:notification.events}

spring.mail.host=${SMTP_HOST:localhost}
spring.mail.port=${SMTP_PORT:1025}
spring.mail.username=${SMTP_USERNAME:}
spring.mail.password=${SMTP_PASSWORD:}
spring.mail.properties.mail.smtp.auth=${SMTP_AUTH:false}
spring.mail.properties.mail.smtp.starttls.enable=${SMTP_STARTTLS:false}

app.public-base-url=${PUBLIC_BASE_URL:http://localhost:8090}
app.mail.from=${MAIL_FROM:no-reply@secret-santa.local}
